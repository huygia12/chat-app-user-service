# Open-api
#
open-api:
  version: v1.0.0
  server:
    url: "${PROTOCOL}://${DOMAIN}:${PORT}"
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
  show-actuator: false
  paths-to-match: /healthcheck, /api/**
